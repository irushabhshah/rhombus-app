apiVersion: v1
kind: Service
metadata:
  name: test-app-service
spec:
  selector:
    app.kubernetes.io/name: test-app-deployment
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30001
  type: NodePort
